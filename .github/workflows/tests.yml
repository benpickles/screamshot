name: CI

on: [push]

jobs:
  rspec:
    name: RSpec
    runs-on: ubuntu-latest
    env:
      API_TOKEN: qwerty
    steps:
    - uses: actions/checkout@v2
    - uses: ruby/setup-ruby@v1
      with:
        bundler-cache: true
        ruby-version: 3.1.0
    - run: bundle exec rspec
